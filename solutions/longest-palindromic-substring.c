#include "solution.h"
/*
Input: "babad"
Output: "bab"
Note: "aba" is also a valid answer.

Input: "cbbd"
Output: "bb"
*/
char *longestPalindrome(char *s)
{
  char *current_str = NULL, *stack = NULL;
  char *stack_tail = NULL;
  char c = '\0';
  for (int i = 0; c != '\0'; i++)
  {
    for (int j = i + 1; c != '\0'; j++)
    {
    }
  }
  return current_str;
}